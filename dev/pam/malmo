# malmo PAM service stack — installed to /etc/pam.d/malmo by the host installer.
# v1: just password auth against the local user database, no extras.
auth     required  pam_unix.so
account  required  pam_unix.so
